const getters = {
  coins: state => state.coins,
  gems: state => state.gems,
  art: state => state.art,
  roll: state => state.roll,
  magic: state => state.magic,
  crateItems: state => state.crateItems
}

export default getters
